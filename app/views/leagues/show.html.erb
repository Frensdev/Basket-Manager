<p id="notice"><%= notice %></p>

<p>
  <strong>Season:</strong>
  <%= @league.season_id %>
</p>

<table>
  <thead>
    <tr>
      <th>jornada</th>
      <th>League</th>
      <th>Season</th>
      <th>Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @jornadas.each do |jornada| %>
      <tr>
        <td><%= jornada.id %></td>
        <td><%= jornada.league_id %></td>
        <td><%= jornada.season_id %></td>
        <td><%= jornada.date %></td>
        <td><%= link_to 'Show', jornada %></td>
        <td><%= link_to 'Edit', edit_jornada_path(jornada) %></td>
        <td><%= link_to 'Destroy', jornada, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Jornada', new_jornada_path %>

<%= link_to 'Edit', edit_league_path(@league) %> |
<%= link_to 'Back', leagues_path %>

<%= link_to 'teams', teams_path(@teams) %>

<%= link_to 'seasons', seasons_path(@seasons) %>

<%= link_to 'jorandas', jornadas_path(@jornadas) %>


#tabla puntos
<table>
  <thead>
    <tr>
      <th>team id</th>
      <th>player id</th>
      <th>puntos</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
    <% @results.each do |result| %>
      team1 
    <% end %>


      </tr>
    <% end %>
  </tbody>
</table>