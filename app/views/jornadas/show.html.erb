<p id="notice"><%= notice %></p>

<p>
  <strong>League:</strong>
  <%= @jornada.league_id %>
</p>

<p>
  <strong>Season:</strong>
  <%= @jornada.season_id %>
</p>

<p>
  <strong>Date:</strong>
  <%= @jornada.date %>
</p>

<table>
  <thead>
    <tr>
      <th>Jornada</th>
      <th>Hometeam</th>
      <th>home result</th>
      <th>away result</th>
      <th>Awayteam</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @matches.each do |match| %>
      <tr>
        <td><%= match.jornada_id %></td>
       
        
       <% if @results.count >= 1 %>   
          <% @results.each do |result| %>
              <% if result.match_id == match.id %>
              
              
                    <% if result.player_id == -1%>
                    <td><%= match.hometeam_id %></td>
                    <td><%= result.puntos %></td>
                    <% end %>
                    
                    <% if result.player_id == -2%>
                   <td><%= result.puntos %></td>
                   <td><%= match.awayteam_id %></td>
                    <% end %>
              <% end %>
          <% end %>
      <% end %>
        <td><%= link_to 'Show', match %></td>
        <td><%= link_to 'Edit', edit_match_path(match) %></td>
        <td><%= link_to 'Destroy', match, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Edit', edit_jornada_path(@jornada) %> |
<%= link_to 'Back', jornadas_path %>

<%= link_to 'matches', matches_path(@matches) %>


#tabla puntos
<table>
  <thead>
    <tr>
      <th>team id</th>
      <th>player id</th>
      <th>puntos</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
    <% @puntos.each do |punto| %>
      <td><%= punto.player_id%></td>
      <td><%= punto.team_id %></td>
      <td><%= punto.puntos %></td>

      </tr>
    <% end %>
  </tbody>
</table>

#tabla rebotes

<table>
  <thead>
    <tr>
      <th>team id</th>
      <th>player id</th>
      <th>rebotes</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
    <% @rebotes.each do |punto| %>
      <td><%= punto.player_id%></td>
      <td><%= punto.team_id %></td>
      <td><%= punto.rebotes + punto.rebotesoff %></td>

      </tr>
    <% end %>
  </tbody>
</table>

#tabla valoración
<table>
  <thead>
    <tr>
      <th>team id</th>
      <th>player id</th>
      <th>valoracion</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
    <% @valoracion.each do |punto| %>
      <td><%= punto.player_id%></td>
      <td><%= punto.team_id %></td>
      <td><%= punto.valoracion %></td>

      </tr>
    <% end %>
  </tbody>
</table>